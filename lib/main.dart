import 'package:flutter/material.dart';

void main() => runApp(MaterialApp(
      title: "Test",
      home: Scaffold(
        appBar: AppBar(),
        body: Container(
          child: Text("Hello World"),
        ),
      ),
    ));
